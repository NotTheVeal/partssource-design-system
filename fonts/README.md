# fonts/

Source Sans 3 is loaded from Google Fonts in `colors_and_type.css`. If you have licensed Source Sans Pro webfont files, drop them here and edit the `@import` at the top of `colors_and_type.css`.

Weights used: 300 (Light), 400 (Regular), 600 (Semibold), 700 (Bold).
